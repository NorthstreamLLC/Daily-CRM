# dependencies
node_modules/
.pnp
.pnp.js

# builds
.next/
out/
build/
dist/

# secrets - never commit these
.env
.env*.local
.env.production
*.pem

# supabase local
.branches
.temp
supabase/.env

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# editors / os
.DS_Store
Thumbs.db
.vscode/
.idea/

# data exports - real player data, keep it out of git
*.csv
!docs/**/*.csv
exports/
imports/

# database backups - every player, every message, every figure, in plain text.
# Migrations under supabase/migrations are still tracked; these are not.
*backup*.sql
*dump*.sql
backups/

# vercel
.vercel

# typescript incremental build cache
*.tsbuildinfo
